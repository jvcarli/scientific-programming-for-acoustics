# Python
.venv
venv
.venv/
venv/

# Localization files that are
# generated by `$ sphinx-build`
# when locale files are read.
src/_locales/**/*.mo

# Sphinx build
# NOTE: Sphinx build directory can be defined by
#       the user when using:
#       `$ sphinx-build website <build-dir>`
#       but here are listed common ones.
build/
.build/
_build/
